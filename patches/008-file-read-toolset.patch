diff --git a/hermes_cli/setup_quick.py b/hermes_cli/setup_quick.py
--- a/hermes_cli/setup_quick.py
+++ b/hermes_cli/setup_quick.py
@@ -178,7 +178,21 @@ def _blank_slate_minimal_toolsets(config: dict):
             # here causes model_tools to subtract their tools (terminal, read_file, …) from the minimal
             # Blank Slate surface (#57315).
             all_keys.add(k)
-        disabled = sorted(all_keys - keep)
+        disabled = all_keys - keep
+
+        # Exclude toolsets whose tools are a subset of a kept toolset's tools.
+        # Disabling a subset (e.g. file_read ⊂ file) would silently strip those
+        # tools from the kept superset — model_tools subtracts by tool name (#57315).
+        from toolsets import resolve_toolset
+        kept_tools = set()
+        for ts in keep:
+            kept_tools.update(resolve_toolset(ts))
+        disabled = {
+            ts for ts in disabled
+            if not (set(resolve_toolset(ts)) & kept_tools)
+        }
+
+        disabled = sorted(disabled)
         if disabled:
             config.setdefault("agent", {})["disabled_toolsets"] = disabled
     except Exception as exc:
diff --git a/hermes_cli/tools_config.py b/hermes_cli/tools_config.py
--- a/hermes_cli/tools_config.py
+++ b/hermes_cli/tools_config.py
@@ -54,6 +54,7 @@ CONFIGURABLE_TOOLSETS = [
     ("browser",         "🌐 Browser Automation",       "navigate, click, type, scroll"),
     ("terminal",        "💻 Terminal & Processes",      "terminal, process"),
     ("file",            "📁 File Operations",           "read, write, patch, search"),
+    ("file_read",       "📖 File Read-Only",             "read_file, search_files (no write/patch)"),
     ("code_execution",  "⚡ Code Execution",            "execute_code"),
     ("vision",          "👁️  Vision / Image Analysis",  "vision_analyze"),
     ("video",           "🎬 Video Analysis",            "video_analyze (requires video-capable model)"),
diff --git a/toolsets.py b/toolsets.py
--- a/toolsets.py
+++ b/toolsets.py
@@ -122,6 +122,11 @@ TOOLSETS = {
         "search (content + files)",
         ["read_file", "write_file", "patch", "search_files"],
     ),
+    "file_read": _ts(
+        "Read-only file tools: read files and search file contents/names "
+        "(no write, patch, or create)",
+        ["read_file", "search_files"],
+    ),
     "tts": _ts("Text-to-speech: convert text to audio with Edge TTS (free), ElevenLabs, OpenAI, or xAI", ["text_to_speech"]),
     "todo": _ts("Task planning and tracking for multi-step work", ["todo_list"]),
     "memory": _ts("Persistent memory across sessions (personal notes + user profile)", ["memory"]),
